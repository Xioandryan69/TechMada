La base de donnee est dans le dossier base

Changer tout les options dans ```.env``` pour la connexion a la base de donnee

lancer la commande ```php -S localhost:8000 -t public```

Le to do list complet de tout le projet se trouve dans le dossier ```TODO.md```